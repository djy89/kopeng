node_modules/
dist/
data/
logs/
models/
*.db
*.db-wal
*.db-shm
.env

# Machine-local Claude Code state — tool-written files can embed secrets in
# approved commands; never track. (.claude/commands/ stays tracked: hand-
# authored docs, not tool-written state.)
.claude/settings.local.json
.claude/settings.json

# Eval comparison artifacts (transient, regenerated by the eval harness)
EVAL-*.md

# Harness scratch output — regenerated on demand
scratch/

# Local MCP config — may hold tokens; never commit
.mcp.json

# Local agent context — machine-local, never public
.agents/

# Migration backups (may hold secrets)
*.bak-tierB
