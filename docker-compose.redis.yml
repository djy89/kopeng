services:
  redis:
    image: redis:7-alpine
    container_name: kopeng-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - kopeng-redis-data:/data

volumes:
  kopeng-redis-data:
